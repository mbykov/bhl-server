server:
  port: 6006
  cert: "../../../bhl/.cert/combined-tma-cert.pem"
  key: "../../../bhl/.cert/tma.local+2-key.pem"

vosk:
  model_path: "../Models/vosk-model-streaming-ru"
  sample_rate: 16000
  feature_dim: 80
  chunk_ms: 100

command:
  enabled: true
  model_path: "../Models/multilingual-e5-small/onnx/model.onnx"
  tokenizer_path: "../Models/multilingual-e5-small/tokenizer.json"
  ort_lib_path: "/home/michael/go/ort/lib/libonnxruntime.so"
  commands_path: "./data/commands.json"
  threshold: 0.92
  min_words: 2

giga:
  enabled: false  # пока заглушка
  model_path: "../Models/gigaam"  # для будущего использования

qwen:
  enabled: false  # пока заглушка
  model_path: "../Models/qwen"

logging:
  verbose: true
  profile: true  # замерять время этапов
